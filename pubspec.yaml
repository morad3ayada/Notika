name: Notika Teacher

description: "تطبيق لإدارة مهام المعلم في مؤسسة نوتيكا التعليمية."

publish_to: 'none' # يمنع نشر الحزمة على pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  file_picker: ^10.2.0
  flutter_sound: ^9.28.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/notika_logo.png
    - assets/ALAnamel.jpg
